<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Auto-updater: required for the system installer flow on Android 8+. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Write downloads onto portable USB drives / removable SD cards.
         Scoped storage can't give libtorrent a File path on those volumes;
         All-files access can. App is sideloaded, so Play policy doesn't apply.
         READ/WRITE_EXTERNAL_STORAGE are declared too so the OEM's "Files and
         media" permission group (with the "Allow management of files" toggle)
         actually surfaces in App Info on Android-TV builds — MANAGE alone is a
         special-access perm that some TV skins never show. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/vnd.android.package-archive" />
        </intent>
    </queries>

    <!-- Android TV requirements -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".TorrentPlayerTvApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:banner="@drawable/banner"
        android:largeHeap="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.TorrentPlayerTv"
        tools:targetApi="r">

        <activity
            android:name=".TvMainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- This is what shows the app on the Android TV launcher.
                     LAUNCHER (without LEANBACK_) wouldn't appear there. -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".details.TvDetailsActivity"
            android:exported="false"
            android:parentActivityName=".TvMainActivity" />

        <activity
            android:name=".search.TvSearchActivity"
            android:exported="false"
            android:parentActivityName=".TvMainActivity" />

        <activity
            android:name=".player.TvPlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout"
            android:theme="@style/Theme.TorrentPlayerTv.Player" />

        <activity
            android:name=".downloads.TvDownloadsActivity"
            android:exported="false"
            android:parentActivityName=".TvMainActivity" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:parentActivityName=".TvMainActivity" />

        <activity
            android:name=".torbox.TvTorBoxLibraryActivity"
            android:exported="false"
            android:parentActivityName=".SettingsActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
