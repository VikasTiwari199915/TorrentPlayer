<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base theme: Material 3 Dynamic Colors for "Material You" support on Android 12+ -->
    <style name="Base.Theme.TorrentPlayer" parent="Theme.Material3.DynamicColors.DayNight.NoActionBar">
        <item name="colorPrimary">@color/seed</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:enforceStatusBarContrast" tools:targetApi="29">false</item>
        <item name="android:enforceNavigationBarContrast" tools:targetApi="29">false</item>
    </style>

    <style name="Theme.TorrentPlayer" parent="Base.Theme.TorrentPlayer" />

    <!-- Detail screen: transparent status bar so backdrop bleeds under it -->
    <style name="Theme.TorrentPlayer.Detail" parent="Theme.TorrentPlayer">
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Player: fullscreen black -->
    <style name="Theme.TorrentPlayer.Player" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Rounded poster shape used in discover carousels -->
    <style name="Shape.Poster" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>

    <!-- Discover section header -->
    <style name="Discover.SectionTitle" parent="TextAppearance.Material3.TitleMedium">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">16dp</item>
        <item name="android:layout_marginEnd">16dp</item>
        <item name="android:layout_marginTop">20dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Quality chip styling -->
    <style name="Chip.Quality" parent="Widget.Material3.Chip.Assist">
        <item name="chipMinHeight">24dp</item>
        <item name="android:textSize">11sp</item>
        <item name="chipStartPadding">8dp</item>
        <item name="chipEndPadding">8dp</item>
    </style>

</resources>
